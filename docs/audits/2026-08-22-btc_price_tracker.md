---
title: "Repo Audit — btc_price_tracker — 2026-08-22"
status: active
created: 2026-08-22
last_reviewed_on: 2026-08-22
review_in: 3 months
applies_to: btc_price_tracker
---

# Repo Health Audit — btc_price_tracker

**Date:** 2026-08-22 · **Lens version:** 1 · **Anchor:** 77/100 · **Files:** 70

> **Read the anchor with care.** It is deterministic and reproducible, so it is the figure to
> trend — but this run established that its dominant term is unreliable. See §0.

## 0. Ruler caveat (new this run)

The god-file penalty is **capped at 40** and all three low-scoring repos hit that cap, so the
anchor saturates instead of discriminating. 2026 evidence does not support raw line-count as an
independent defect predictor — its effect runs through *coupling*, and **churn** is the far
stronger signal ([arXiv 2106.04687](https://arxiv.org/pdf/2106.04687),
[Microsoft Research](https://www.microsoft.com/en-us/research/wp-content/uploads/2016/02/icse05churn.pdf)).
The dead-module heuristic measured **~65% false positives** ecosystem-wide. Treat both as lead
lists, not verdicts. Full reasoning: [ecosystem rollup](../../../docs/audits/2026-08-22-ECOSYSTEM-ROLLUP.md).

**Anchor breakdown:** god_file 19 · large_file 4 · dead 0 · artifacts 0 · gitignore 0

**Primary bottleneck:** Unpinned indicator dependencies under a declared public-API contract.

## 1. Since last audit

*First audit — this becomes the baseline.*

## 2. Findings

### 🧹 Janitor
| Path | Evidence | Sev | Conf |
|---|---|---|---|
| `bin/btc-{daily,hourly}.sh`, `bin/notify.sh` | **[verified]** No plist references them — all `deploy/*.plist` call `venv/bin/python bin/run_*.py`. Also hardcode a stale "18 tokens" (actual: 17, TON removed). The Python-import heuristic cannot see shell entrypoints. | med | high |
| `images/Research_01.png` (1.7MB) | **[verified]** Zero references in this repo. NOTE: the identically-named file in eeva-exec **is** used — do not generalise. | low-med | high |
| `title_image/ChatGPT Image ….png` (2.1MB) | **[verified]** Referenced by README.md — **NOT dead**, the agent's flag was wrong. | — | — |
| `run_daily.py:6` | Docstring says "01:05 UTC"; actual launchd schedule is 03:10 local. | low | high |

### 🏛️ Architect — *verdict: layered app, mud only inside `pipeline.py`*
| Path | Evidence | Sev | Conf |
|---|---|---|---|
| `pipeline.py` (902) | `run_seed`/`run_perp_seed`, `run_update`/`run_perp_update`, `run_backfill`/`run_perp_backfill` are structurally identical pairs — ~350 duplicated lines. Seam: one `market_type` param (the pattern `db.py` already uses cleanly). | high | high |
| `indicators.py` (1447) | 700-line static glossary dict + 750 lines of computation. Pure move: split `indicator_glossary.py`. | med | high |
| `db.py:167`, `pipeline.py:889` | Two documented in-function imports to dodge a `db`↔`indicators` cycle. A `glossary.py` owned by neither removes both. | med | high |

### 🔬 Clean Code
| File:line | Evidence | Sev | Conf |
|---|---|---|---|
| `extract_perp.py:63-74` | **Reliability asymmetry:** `extract.py` retries 3× with backoff on KuCoin's misclassified `429000`; the perp path has **no retry at all**. `lean_pipeline.py` re-invented its own `_retry()` around the perp fetchers — direct evidence the gap is real. | med-high | high |
| `indicators.py:1256-1259` | `_compute_supertrend`: outer `elif` matches 3 prefixes, inner `if` acts on 1 — silent no-op for `SUPERTl`/`SUPERTs`. Correct today, misleading. | low | high |

### ⚙️ DevOps
| Evidence | Sev | Conf |
|---|---|---|
| **[verified]** `requirements.txt` pins **only** `ccxt==4.5.40`. `pandas`, `pandas-ta-classic`, `numpy`, `pymongo` all float, no lockfile. `pandas-ta-classic` computes the *values* under column names ADR-001 declares a public API — a routine reinstall across four surfaces (venv, 2× GH Actions, Dockerfile) can change them silently. Names stay stable, so nothing downstream notices. | **high** | high |
| No `pytest` on push/PR — both workflows are `workflow_dispatch`-only. 100+ tests never run automatically. | med | high |
| No `.env.example` at all. | med | high |
| **[verified]** Watchdog **does** cover weekly (`("1w","spot"):1`, commit `fc66267`) — corrects a stale ecosystem memory. Residual gap is WIF's 2 known bars only. | — | high |

## 3. Priority actions

| # | Action | Effort | Impact |
|---|---|---|---|
| 1 | **Pin `pandas`, `pandas-ta-classic`, `numpy`, `pymongo` + commit a lockfile** — protects the public indicator contract from silent value drift | S | **high** |
| 2 | Add retry/backoff to `extract_perp.py` fetchers, matching `extract.py` | S | high |
| 3 | Delete the 3 dead `bin/*.sh` (stale "18 tokens" is a live footgun) | S | med |
| 4 | Add `pytest` on PR | S | med-high |
| 5 | Collapse `pipeline.py` spot/perp duplication behind `market_type` | L | high |

---
*Generated by `/crucible:repo-audit`. Lenses frozen (v1); anchor deterministic. Claims marked
**[verified]** were independently re-derived in the main session; unmarked claims are the lens
agent's and carry its stated confidence.*
