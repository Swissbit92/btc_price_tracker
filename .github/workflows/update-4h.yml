name: 4-Hour Price Update (All Tokens)

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  update-4h:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run 4h update for all tokens (spot)
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
        run: python update.py --all --timeframe 4h

      - name: Run 4h update for all tokens (perp)
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
        run: python update.py --all --timeframe 4h --market-type perp
