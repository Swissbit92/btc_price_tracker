# Core dependencies
ccxt==4.5.40
pandas
pandas-ta-classic
numpy
pymongo
python-dotenv
requests

# HTTP / Flask (Cloud Run fallback)
flask
gunicorn

# MCP server (Claude Code integration)
mcp

# Testing
pytest
